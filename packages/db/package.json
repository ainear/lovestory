{
    "name": "@lovestory/db",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "type-check": "tsc --noEmit",
        "db:push": "drizzle-kit push",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "drizzle-kit migrate",
        "db:studio": "drizzle-kit studio",
        "db:seed": "tsx src/seed/index.ts"
    },
    "dependencies": {
        "@lovestory/shared": "workspace:*",
        "drizzle-orm": "^0.39.0",
        "postgres": "^3.4.0"
    },
    "devDependencies": {
        "@types/node": "^20",
        "drizzle-kit": "^0.30.0",
        "tsx": "^4.19.0",
        "typescript": "^5"
    }
}