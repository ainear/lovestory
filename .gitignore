# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
.next
out
build
.turbo

# Environment – NEVER commit real env files
.env
.env.local
.env.*.local
.env.dev
.env.prod
.env.production
.env.staging
.env.*
# Keep only .env.example
!.env.example

# IDE / OS
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Debug logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Testing & reports
coverage
test-results/
playwright-report/
.playwright-mcp/
*-report.json
k6-stress-report.json

# Debug screenshots
/*.png
apps/web/tests/e2e/screenshots/

# Drizzle
drizzle/meta

# AI Agent temp, logs & caches (ignore logs/cache only, KEEP rules, skills, workflows)
.gemini/
.claude/
.cursorrules
.agent/logs/
.agent/cache/
.agents/logs/
.agents/cache/
.codegraph/

# Backups
backups/

# ─── SECURITY: credentials & keys ─────────────────────────────────────────────
# Private keys
*.pem
*.key
*.p8
*.p12
*.pfx
*.cer

# Android keystores
*.jks
*.keystore

# Firebase / Google Service Account
google-services.json
GoogleService-Info.plist
*-service-account*.json
*service_account*.json
firebase-adminsdk*.json
serviceAccountKey*.json

# Supabase temp / local CLI state (contains project-ref, pool URLs)
supabase/.temp/

# Generic
*credentials*.json
*secret*.json
*secrets*.json
*private*.json

# ─── Mobile build artefacts (large binary, no need in repo) ───────────────────
*.apk
*.aab
*.ipa
*.dSYM
*.dSYM.zip

# Video / large media / archives
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.tar.gz
*.rar
*.7z

# ─── Vercel ───────────────────────────────────────────────────────────────────
.vercel
